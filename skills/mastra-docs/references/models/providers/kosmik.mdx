---
title: "Kosmik Compute Models: IDs, Pricing & Usage"
description: "Browse Kosmik Compute model IDs, capabilities, context windows, and pricing. Learn how to use them in TypeScript with Mastra's model router."
---

# [Kosmik Compute logo]Kosmik Compute

Access 1 Kosmik Compute model through Mastra's model router. Authentication is handled automatically using the `KOSMIK_API_KEY` environment variable.

Learn more in the [Kosmik Compute documentation](https://api.koscompute.com/docs/).

```bash title=".env"
KOSMIK_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "kosmik/qwen/qwen3.8-27b"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

> **note**: Mastra uses the OpenAI-compatible `/chat/completions` endpoint. Some provider-specific features may not be available. Check the [Kosmik Compute documentation](https://api.koscompute.com/docs/) for details.

## Models

<ProviderModelsTable
  models={[
  {
    "model": "kosmik/qwen/qwen3.8-27b",
    "imageInput": true,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 262144,
    "maxOutput": 32768,
    "inputCost": 0.35,
    "outputCost": 2.2
  }
]}
/>

Model availability, capabilities, context windows, and pricing are sourced from [models.dev](https://models.dev) and may change.

## Advanced configuration

### Custom headers

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://api.koscompute.com/v1",
    id: "kosmik/qwen/qwen3.8-27b",
    apiKey: process.env.KOSMIK_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### Dynamic model selection

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "kosmik/qwen/qwen3.8-27b"
      : "kosmik/qwen/qwen3.8-27b";
  }
});
```
