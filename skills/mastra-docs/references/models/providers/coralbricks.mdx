---
title: "CoralBricks | Models"
description: "Use CoralBricks models with Mastra. 3 models available."
---

# [CoralBricks logo]CoralBricks

Access 3 CoralBricks models through Mastra's model router. Authentication is handled automatically using the `CORAL_API_KEY` environment variable.

Learn more in the [CoralBricks documentation](https://www.coralbricks.ai/docs).

```bash title=".env"
CORAL_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "coralbricks/glm-5.3-fp4"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

> **note**: Mastra uses the OpenAI-compatible `/chat/completions` endpoint. Some provider-specific features may not be available. Check the [CoralBricks documentation](https://www.coralbricks.ai/docs) for details.

## Models

<ProviderModelsTable
  models={[
  {
    "model": "coralbricks/glm-5.3-fp4",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 1048576,
    "maxOutput": 131072,
    "inputCost": 1.12,
    "outputCost": 4.4
  },
  {
    "model": "coralbricks/gpt-oss-120b",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 131072,
    "maxOutput": 32768,
    "inputCost": 0.12,
    "outputCost": 0.6
  },
  {
    "model": "coralbricks/kimi-k3",
    "imageInput": true,
    "audioInput": false,
    "videoInput": true,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 1048576,
    "maxOutput": 131072,
    "inputCost": 3,
    "outputCost": 15
  }
]}
/>

## Advanced configuration

### Custom headers

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://inference.coralbricks.ai/v1",
    id: "coralbricks/glm-5.3-fp4",
    apiKey: process.env.CORAL_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### Dynamic model selection

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "coralbricks/kimi-k3"
      : "coralbricks/glm-5.3-fp4";
  }
});
```
