---
title: "Modal | Models"
description: "Use Modal models with Mastra. 4 models available."
---

# [Modal logo]Modal

Access 4 Modal models through Mastra's model router. Authentication is handled automatically using the `MODAL_PROXY_TOKEN` environment variable.

Learn more in the [Modal documentation](https://modal.com/docs/guide/endpoints).

```bash title=".env"
MODAL_PROXY_TOKEN=your-api-token
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "modal/Qwen/Qwen3.8-2.4T-A95B"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

> **note**: Mastra uses the OpenAI-compatible `/chat/completions` endpoint. Some provider-specific features may not be available. Check the [Modal documentation](https://modal.com/docs/guide/endpoints) for details.

## Models

<ProviderModelsTable
  models={[
  {
    "model": "modal/moonshotai/Kimi-K3",
    "imageInput": true,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 1048576,
    "maxOutput": 131072,
    "inputCost": 3,
    "outputCost": 15
  },
  {
    "model": "modal/Qwen/Qwen3.8-2.4T-A95B",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 1010000,
    "maxOutput": 131072,
    "inputCost": 2,
    "outputCost": 6
  },
  {
    "model": "modal/thinkingmachines/Inkling-NVFP4",
    "imageInput": true,
    "audioInput": true,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 1048576,
    "maxOutput": 262144,
    "inputCost": 1.2,
    "outputCost": 5
  },
  {
    "model": "modal/zai-org/GLM-5.3-Flash",
    "imageInput": true,
    "audioInput": false,
    "videoInput": true,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 1000000,
    "maxOutput": 131072,
    "inputCost": 0.45,
    "outputCost": 1.5
  }
]}
/>

## Advanced configuration

### Custom headers

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://inference.us-west.modal.direct/v1",
    id: "modal/Qwen/Qwen3.8-2.4T-A95B",
    apiKey: process.env.MODAL_PROXY_TOKEN,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### Dynamic model selection

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "modal/zai-org/GLM-5.3-Flash"
      : "modal/Qwen/Qwen3.8-2.4T-A95B";
  }
});
```
