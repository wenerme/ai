---
title: "Reference: Mastra.listVectors() | Core"
description: "The .listVectors() method is used to retrieve all vector stores that have been configured in the Mastra instance."
packages:
  - "@mastra/core"
---

# Mastra.listVectors()

The `.listVectors()` method is used to retrieve all vector stores that have been configured in the Mastra instance.

## Usage example

```typescript
mastra.listVectors()
```

## Parameters

This method doesn't accept any parameters.

## Returns

<PropertiesTable
  content={[
  {
    name: 'vectors',
    type: 'TVectors',
    description:
      'A record of all configured vector stores, where keys are vector store names and values are vector store instances.',
  },
]}
/>

## Related

- [Vector stores overview](/reference/rag/vector-databases)
- [RAG overview](/reference/rag/overview)
