---
title: "Reference: Workflow.parallel() | Workflows"
description: "Use Workflow.parallel() to run multiple steps concurrently and wait for every branch before passing their combined results to later workflow steps."
packages:
  - "@mastra/core"
---

# Workflow\.parallel()

The `.parallel()` method executes multiple steps in parallel.

## Usage example

```typescript
workflow.parallel([step1, step2])
```

## Parameters

<PropertiesTable
  content={[
  {
    name: 'steps',
    type: 'Step[]',
    description: 'The step instances to execute in parallel',
    isOptional: false,
  },
]}
/>

## Returns

<PropertiesTable
  content={[
  {
    name: 'workflow',
    type: 'Workflow',
    description: 'The workflow instance for method chaining',
  },
]}
/>

## Related

- [Simultaneous steps with parallel](/docs/workflows/control-flow#simultaneous-steps-with-parallel)
