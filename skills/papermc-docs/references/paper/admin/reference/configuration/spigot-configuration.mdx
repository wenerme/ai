---
title: spigot.yml
description: An explanation of Spigot's configuration file.
tableOfContents: false
---

> **note**: The below YAML shows you the structure and default values for `spigot.yml`

Click on a leaf node to view the description for that setting.

> **note**: To override a per-world option, create a new key under `world-settings` with the level-name (name of the folder).

<ConfigNode data={parse(SpigotYML)} />
