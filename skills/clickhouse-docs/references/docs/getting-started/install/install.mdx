---
description: 'Install ClickHouse'
keywords: ['clickhouse', 'install', 'getting started', 'quick start']
title: 'Installation instructions'
doc_type: 'guide'
---

<CardPrimary
    alignContent="center"
    description="Start building instantly without having to size and scale your cluster. We take care of sizing, scaling
    , security, reliability and upgrades."
    icon="cloud"
    infoText="Get started free"
    infoUrl="https://console.clickhouse.cloud/signUp?loc=docs-install-page-banner"
    isSelected
    onButtonClick={galaxyOnClick('docs.installCloudCallout.buttonClicked')}
    size="md"
    title="ClickHouse Cloud"
    topBadgeText="Recommended"
/>

Alternatively, select a platform, distribution and install method below to view install
instructions for Opensource ClickHouse:

<InstallSelector
    cli={<CLIInstall/>}
    windows={<Windows/>}
    quickinstall={<QuickInstall/>}
    debian_prod={<DebianProd/>}
    rpm_prod={<RPMProd/>}
    tar_prod={<TarProd/>}
    macos_prod={<MacOSProd/>}
    nixos={<NixOS/>}
    docker={<Docker/>}
/>
